type CodeSample = {
  id: number;
  name: string;
  codeSample: string;
};

type Library = {
  name: string;
  url: string;
  use: string;
  version: string;
};

type HistoryItem = {
  code: string;
  date: string;
};
