interface CodeSample {
  id: number;
  name: string;
  codeSample: string;
}

interface EditorTheme {
  id: number;
  value: Theme;
  icon: string;
}

interface Library {
  name: string;
  url: string;
  use: string;
  version: string;
}
